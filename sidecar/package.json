{
	"name": "@winthorpe/sidecar",
	"description": "Winthorpe's agent-orchestration sidecar process (Claude Agent SDK + Codex SDK).",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "bun run scripts/stage-vendor.ts && bun build --compile src/index.ts --outfile dist/winthorpe-sidecar",
		"dev:prepare": "bun run stage-vendor",
		"stage-vendor": "bun run scripts/stage-vendor.ts",
		"dev": "bun run src/index.ts",
		"test": "bun test",
		"typecheck": "bunx tsc --noEmit"
	},
	"dependencies": {
		"@anthropic-ai/claude-agent-sdk": "0.2.111",
		"@anthropic-ai/claude-code": "2.1.111",
		"@openai/codex": "0.124.0"
	},
	"devDependencies": {
		"@types/bun": "^1.3.11",
		"@types/node": "^25.5.2"
	}
}
